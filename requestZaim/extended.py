import zaim
  
class ExtendedZaim(object):
    def __init__( z ):
        if isinstance(z , zaim):
            self._zaim = z

    def genreByCategory(category_id):
        genres=''
	return genres

    def genreByCategory(genre_id):
        genres=''
	return genres

    def searchFromLatestToHistoryDate(history_date):

	self._zaim.categories()[u,'categories']
	for catregory in categories:
             tmpReceipts = self._zaim.search(genre_id=category[u,'genre_id'])

        receipts = ''
	return receipts
